[ITER] 0
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ;}  move} ; C ; END with cost 0
all rewards: [0, 0, 0, 0, 0]
current time: 52.678874492645264
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 4 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 105.58146238327026
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 4 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 159.66126680374146
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 4 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 1
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.16
current time: 212.7169258594513
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 4 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 264.9313201904297
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 285.54820013046265
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 306.22339510917664
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 326.5900197029114
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 346.889018535614
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 367.55622148513794
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END
[ITER] 2
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.16
current time: 389.3152918815613
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 4 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 441.8417909145355
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 461.66366505622864
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 482.27271914482117
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 502.70042657852173
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 522.7822768688202
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 543.3266308307648
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END
[ITER] 3
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.16
current time: 564.3675632476807
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 4 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 616.5075924396515
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 636.7698576450348
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 656.7079570293427
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 676.9278743267059
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 698.7792389392853
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END
current time: 720.0247178077698
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END
[ITER] 4
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.16
current time: 741.3399560451508
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 763.2374806404114
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 784.8642511367798
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
unable to put into queue and get cost 4 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 866.9127900600433
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 888.1245834827423
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 909.6594262123108
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 930.9631474018097
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 953.00861287117
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 1016.3178896903992
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
[ITER] 5
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.16
current time: 1104.570599079132
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1126.4894824028015
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1147.9709570407867
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
unable to put into queue and get cost 4 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1230.5310661792755
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1251.8481929302216
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1273.806247472763
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1295.2569131851196
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 1317.5681312084198
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 1379.071087360382
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
[ITER] 6
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.16
current time: 1468.4910221099854
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1489.4857976436615
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1511.2657709121704
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
unable to put into queue and get cost 4 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1592.7322878837585
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1613.7612924575806
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1635.2717444896698
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1656.3905539512634
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 1678.4695763587952
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 5 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 1739.9318537712097
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
[ITER] 7
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.2
current time: 1827.1968235969543
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 1848.0315010547638
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 1880.4708786010742
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 8
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.2
current time: 1902.2474386692047
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 1923.8631384372711
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
current time: 1945.2530229091644
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 9
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.2
current time: 1979.4019935131073
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2000.5819835662842
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2021.509626865387
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 10
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.2
current time: 2054.4618129730225
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
current time: 2076.3291795253754
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 2098.769422531128
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 11
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.2
current time: 2131.6162672042847
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 2152.6414227485657
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 2185.596837282181
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 12
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.2
current time: 2206.6210930347443
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 2239.469619035721
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 2259.6609420776367
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 13
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.2
current time: 2280.822103023529
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 2302.907778739929
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 2332.973090171814
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 14
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.2
current time: 2355.060895681381
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 2376.9307129383087
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 2411.9987773895264
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 15
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.2
current time: 2434.031416416168
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 2472.5759880542755
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 2510.8209981918335
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 2600.023728132248
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 2639.2748687267303
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} } }  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} } }  move} ; C ; END
[ITER] 16
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.2
current time: 2727.134677886963
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2757.6220791339874
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2779.8369195461273
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 17
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.2
current time: 2801.401661634445
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2823.253211259842
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 2845.4273631572723
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 18
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.2
current time: 2880.3975615501404
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 2919.1708953380585
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 2958.0129673480988
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 3045.0871255397797
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 3084.5532174110413
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} } }  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} } }  move} ; C ; END
[ITER] 19
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.2
current time: 3170.6651623249054
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 3192.9450902938843
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 3214.65469455719
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 20
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.2
current time: 3246.6771359443665
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 3268.2722656726837
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 3302.9770317077637
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 21
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.2
current time: 3324.8644239902496
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 3364.2735900878906
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 3402.8883769512177
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
unable to put into queue and get cost 5 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 3491.1649401187897
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 3529.9915499687195
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} } }  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} } }  move} ; C ; END
[ITER] 22
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 3617.839416742325
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 3639.6209831237793
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 3663.6149537563324
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 23
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 3684.8512976169586
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 3705.7423553466797
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 3726.7012696266174
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 24
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 3797.8732063770294
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 3819.133015871048
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
current time: 3840.2958240509033
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 25
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 3862.9406356811523
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
current time: 3932.362809896469
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END
current time: 3952.9537827968597
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END
------ end current search -------

[ITER] 26
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 3973.898998737335
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 3995.3656356334686
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 4018.7136328220367
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 27
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 4039.9864099025726
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 4111.459180355072
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 4133.011881351471
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 28
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 4154.5405695438385
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 4177.810844659805
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
current time: 4199.150007247925
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 29
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 4222.588093757629
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
current time: 4293.262229681015
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END
current time: 4314.045928239822
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END
------ end current search -------

[ITER] 30
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 4335.501110076904
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 4357.001190662384
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 4378.438502550125
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 31
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 4401.89747428894
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 4422.99108171463
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 4492.42916226387
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 32
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 4513.8726053237915
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END
current time: 4535.194420337677
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END
current time: 4556.295345544815
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 33
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; C }  move} ; C ; END with cost 0.24
current time: 4579.94035077095
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ;}  move} ; C ; END
current time: 4653.294819355011
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END
current time: 4673.520436763763
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END
------ end current search -------

[ITER] 34
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 4694.87341594696
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4717.314516544342
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4747.898597002029
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 35
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 4770.028435468674
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4792.008067846298
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4817.439180850983
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 36
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 4839.658623933792
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4874.933264017105
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4896.659352064133
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 37
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 4918.321095228195
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4940.685786962509
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 4962.795456409454
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 38
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 4987.587490081787
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5017.82196354866
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5040.116806983948
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 39
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5062.273496389389
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5087.773500442505
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5109.919251203537
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 40
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5131.558780908585
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5153.976979017258
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5178.626339912415
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 41
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5213.318143606186
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5235.3872764110565
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5259.674348592758
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 42
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 5281.959755182266
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5311.880807876587
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5333.82392168045
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 43
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 5355.886210441589
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5378.418558597565
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5403.927324295044
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 44
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 5426.018213510513
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5457.8225383758545
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5477.715228319168
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 45
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 5497.0206179618835
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5516.060913324356
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 5537.14545083046
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 46
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 5556.484103918076
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 5573.976470947266
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 47
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 5591.679017543793
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 5627.229681253433
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 5645.092879533768
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 5663.646342277527
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 48
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 5682.074872255325
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 5699.77135181427
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 49
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.24
current time: 5719.061472415924
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END
current time: 5736.723536491394
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 50
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.24
current time: 5755.057995080948
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
current time: 5772.827564954758
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 51
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 5790.717705011368
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 5826.743384599686
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 5845.102529525757
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 5862.957594394684
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 52
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5881.806524753571
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5899.597211122513
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 53
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5917.281869888306
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5935.110676050186
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 54
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 5952.935306310654
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 5988.594482898712
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 6006.451346874237
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 6024.331134319305
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 55
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.24
current time: 6042.434723377228
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
current time: 6060.400222301483
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 56
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.24
current time: 6078.1003568172455
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END
current time: 6095.916611671448
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 57
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 6113.526139497757
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 6154.645255565643
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 6177.345522642136
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 6198.109384775162
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 58
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 6219.275678873062
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 6240.213705778122
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 59
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 6262.102658987045
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 6304.744924068451
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 6325.630125761032
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 6346.842208147049
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 60
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 6368.696696519852
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 6389.2784242630005
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 61
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 6409.982877492905
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 6451.89212846756
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 6472.868231296539
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 6494.722627401352
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 62
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.24
current time: 6516.550207853317
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END
current time: 6537.534381151199
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 63
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.24
current time: 6558.911518573761
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
current time: 6581.324570178986
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 64
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6602.607818365097
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6624.592555761337
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 65
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6646.119085073471
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6685.278253316879
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6709.578802347183
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6733.442286491394
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 66
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6757.006154537201
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6779.22970366478
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 67
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6800.711556434631
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6822.795167922974
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 68
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6845.0813574790955
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6887.727674484253
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6911.076087713242
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6934.5371742248535
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 69
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 6958.271797418594
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 6980.0853753089905
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 70
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.24
current time: 7002.278972864151
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 7046.052013158798
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 7085.5366106033325
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 71
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7124.620634555817
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7164.6895389556885
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7188.074942111969
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7212.880023479462
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 72
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7236.725920915604
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7258.865030050278
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 73
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7281.419434547424
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7303.614867448807
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 74
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7325.868333339691
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7347.567578792572
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 75
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7369.548032283783
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7391.900260925293
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 76
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 7413.6161477565765
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7456.427286863327
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7479.376050949097
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 7502.939811229706
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 77
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.24
current time: 7527.154149770737
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 7566.806575059891
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 7605.66147851944
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 78
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 7649.330594539642
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7671.0496282577515
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 79
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 7693.286753177643
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7715.39766907692
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 80
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 7737.61305642128
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7775.877499818802
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7799.97469997406
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7823.487727642059
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 81
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 7847.835359096527
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7870.023559093475
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 82
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 7891.845938444138
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7934.702617645264
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7957.4470908641815
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 7980.91882443428
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 83
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 8006.007401227951
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 8028.399744749069
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 84
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.24
current time: 8051.102157354355
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 8089.889988660812
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 8129.213077545166
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 85
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 8173.166396617889
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 8223.056928634644
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 8260.715046405792
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 8298.621920347214
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 86
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 8336.248799085617
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 8386.930614948273
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 8425.117361068726
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 8462.945356845856
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 87
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 8501.204823732376
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 8550.875578641891
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 8587.98356294632
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 8625.33428812027
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 88
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 8666.484089374542
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 8716.989969491959
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 8754.45475268364
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 8793.316348075867
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 89
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 8831.975666999817
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 8881.745176076889
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 8919.575462579727
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 8957.482643842697
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 90
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 8995.71550321579
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END
current time: 9046.015326499939
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 9084.753473997116
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 9123.548389911652
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 91
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 9161.792975902557
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 9200.523148536682
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 9289.546571731567
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
------ end current search -------

[ITER] 92
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 9352.58072257042
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 9403.194560050964
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 9442.711866378784
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 9481.462228059769
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 93
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 9520.810141324997
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 9584.912640571594
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 9624.276364326477
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 94
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 9711.41719865799
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 9763.099045276642
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 9802.22331571579
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 9841.460508108139
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 95
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 9880.413782596588
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;}  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 9968.157767295837
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 10007.747009038925
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 6 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
------ end current search -------

[ITER] 96
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 10071.798596143723
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 6 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 10125.777852535248
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 10164.46726322174
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 10203.087043523788
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 97
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 10242.910087108612
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 10332.157552480698
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 10382.3718521595
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 98
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 10432.622175216675
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
current time: 10482.934374809265
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
current time: 10534.003821849823
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 1000 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
------ end current search -------

[ITER] 99
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 10620.30541729927
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 10671.042906045914
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 10722.133561611176
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 100
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 10812.100601911545
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
current time: 10862.454336643219
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 1000 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
current time: 10948.656260967255
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
------ end current search -------

[ITER] 101
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 10998.89916729927
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 11088.600898504257
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 11138.853383302689
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 102
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.24
current time: 11189.443400382996
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 1000 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ;}  move} ; C ; END
current time: 11276.107463121414
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
current time: 11326.158613443375
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
------ end current search -------

[ITER] 103
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END with cost 0.24
current time: 11376.877747058868
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 11416.634315729141
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 11456.105705499649
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 11546.463099956512
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 11587.820979356766
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} }}  move} ; C ; END
unable to put into queue and get cost 7 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;} }}  move} ; C ; END
[ITER] 104
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.24
current time: 11676.62425160408
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 11728.057429075241
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 11779.88632941246
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
------ end current search -------

[ITER] 105
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END with cost 0.24
current time: 11868.139651536942
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 11907.99798989296
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 11947.112357378006
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 12034.703697681427
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 12074.698640108109
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }}  move} ; C ; END
unable to put into queue and get cost 7 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }}  move} ; C ; END
[ITER] 106
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.24
current time: 12162.153456926346
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 12213.829236507416
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 12266.185844421387
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
------ end current search -------

[ITER] 107
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END with cost 0.24
current time: 12354.05802154541
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 12394.8128221035
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 12434.799172878265
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 12522.886735200882
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(right_is_clear) { WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 12562.501124382019
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }}  move} ; C ; END
unable to put into queue and get cost 7 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ;} ELSE { IF(not (right_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }}  move} ; C ; END
[ITER] 108
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.24
current time: 12651.849539279938
all rewards: [0, 0, 0, 0, 0]

 no fuel with reward 0 under seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
unable to put into queue and get cost 6 reward 0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 12743.18308520317
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 12794.874712705612
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 109
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.28
current time: 12846.785612821579
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 12885.614421129227
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 12925.125680208206
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 110
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.28
current time: 12969.109227895737
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 13008.336023569107
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 13048.114514112473
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 111
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.28
current time: 13089.946347951889
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 13128.976445198059
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 13173.691216230392
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 112
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.28
current time: 13213.475439548492
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 13252.16226029396
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 13291.845920324326
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 113
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.28
current time: 13332.369323253632
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 13370.711079120636
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 13409.265552520752
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 114
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.28
current time: 13453.305502653122
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 13493.23456954956
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 13533.34936952591
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 115
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 13573.65928554535
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13594.622257947922
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 116
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 13615.664296865463
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13638.783864498138
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13662.130300283432
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 117
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 13714.265931367874
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13735.504813432693
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 118
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 13757.102108240128
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13811.306262731552
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 119
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 13832.84098815918
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 13887.859396457672
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 120
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 13908.763625144958
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
current time: 13930.216886043549
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 121
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 13951.946890830994
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  move} ; C ; END
current time: 13973.34366440773
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 122
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.28
current time: 13995.211470127106
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  move} ; C ; END
current time: 14048.66854596138
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
current time: 14071.770345449448
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
------ end current search -------

[ITER] 123
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END with cost 0.28
current time: 14095.031861066818
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  move} ; C ; END
current time: 14150.247359275818
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  move} ; C ; END
------ end current search -------

[ITER] 124
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END with cost 0.28
current time: 14171.502894163132
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  move} ; C ; END
current time: 14192.366694450378
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  move} ; C ; END
------ end current search -------

[ITER] 125
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 14247.89730834961
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14269.323414087296
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 126
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 14290.32282090187
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14343.594621896744
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14366.635001182556
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 127
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 14389.72192621231
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14411.423498630524
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 128
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 14432.961537122726
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14454.921158790588
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 129
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 14509.484051465988
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 14563.972695350647
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 130
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 14585.07208943367
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  move} ; C ; END
current time: 14606.078593730927
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 131
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 14627.182976007462
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
current time: 14648.67572760582
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 132
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.28
current time: 14670.108515739441
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  move} ; C ; END
current time: 14722.527027845383
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
current time: 14745.209394454956
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
------ end current search -------

[ITER] 133
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END with cost 0.28
current time: 14769.06825184822
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  move} ; C ; END
current time: 14790.802658557892
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  move} ; C ; END
------ end current search -------

[ITER] 134
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END with cost 0.28
current time: 14847.141690969467
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  move} ; C ; END
current time: 14901.68502998352
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  move} ; C ; END
------ end current search -------

[ITER] 135
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 14922.274722099304
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 14943.609471082687
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 136
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 14964.887026071548
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 14984.841201543808
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 137
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 15006.290228128433
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 15029.814892053604
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 15083.667669296265
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 138
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 15107.033529281616
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 15161.767011165619
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 139
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 15183.269666433334
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 15238.395908594131
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 140
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 15259.861639499664
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END
current time: 15280.697348117828
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 141
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 15302.939058303833
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  move} ; C ; END
current time: 15324.109832763672
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 142
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  move} ; C ; END with cost 0.28
current time: 15344.801468610764
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  move} ; C ; END
current time: 15368.771470785141
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;}  move} ; C ; END
current time: 15421.820290327072
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  move} ; C ; END
------ end current search -------

[ITER] 143
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right C }  move} ; C ; END with cost 0.28
current time: 15444.606995105743
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right turn_right C }  move} ; C ; END
current time: 15465.272904872894
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_right}  move} ; C ; END
------ end current search -------

[ITER] 144
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left C }  move} ; C ; END with cost 0.28
current time: 15520.757650375366
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left turn_left C }  move} ; C ; END
current time: 15542.146530151367
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { turn_left WHILE(right_is_clear) { C } ; turn_left}  move} ; C ; END
------ end current search -------

[ITER] 145
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15597.147212982178
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15619.786524772644
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 146
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15641.317188739777
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15680.92510676384
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15704.11450123787
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15728.102438926697
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 147
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15751.971736192703
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15773.70406460762
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 148
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15796.099820613861
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15818.068236112595
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 149
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15840.12732553482
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15866.758175134659
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15891.298891067505
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15915.786516427994
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 150
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15940.481209993362
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 15962.907915353775
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 151
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 15984.642091274261
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16028.133656978607
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16051.42757844925
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16074.98106265068
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 152
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 16098.7019906044
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16120.842891693115
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 153
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 16143.125395536423
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16165.044469594955
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 154
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 16187.61072587967
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16210.290897846222
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 155
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 16232.255281448364
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16254.315069675446
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 156
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 16276.625378608704
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 16301.234297275543
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 16331.507898807526
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 157
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16356.187716245651
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16395.16907310486
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16418.047230005264
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16441.85363483429
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 158
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16466.449018716812
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16488.77352643013
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 159
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16511.344265937805
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16533.943803071976
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 160
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16556.718002319336
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16582.01157593727
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16606.66682767868
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16630.307412862778
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 161
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16654.789527893066
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16676.270803451538
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 162
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16697.573755025864
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16718.80403661728
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 163
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16739.958966255188
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16761.723116397858
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 164
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16783.244935512543
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16804.981463193893
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 165
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16827.132160663605
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16869.357400417328
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16891.906918287277
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16914.64379143715
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 166
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16937.70863699913
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 16959.03568148613
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 167
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 16980.638268232346
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 17004.57078075409
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 17034.315501451492
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 168
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 17058.160638570786
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 17079.29551053047
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 169
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17101.142597913742
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17138.637468099594
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17161.027475118637
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17183.71402001381
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 170
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17206.869517803192
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17228.33519220352
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 171
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17250.127002239227
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17272.12487077713
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { move WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 172
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17293.692222833633
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17315.26060295105
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 173
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17336.152316570282
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17360.737593889236
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17384.740141153336
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17407.644607305527
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 174
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17431.386093854904
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17453.676496505737
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 175
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17475.094694137573
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17517.29833984375
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_right WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17539.99234843254
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17562.58106803894
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 176
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17585.921171188354
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17607.5741686821
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 177
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17629.03126168251
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17650.49724841118
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { move WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 178
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17672.31809782982
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17693.655263900757
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 179
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17715.283941984177
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_left C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17739.360854387283
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ;} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 17769.048634529114
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 180
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 17793.00194978714
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 17814.427144765854
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { turn_right WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 181
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 17835.907382249832
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17856.46446609497
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 182
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 17877.853443145752
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17897.894698619843
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17917.812268972397
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17937.79236149788
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17958.209748506546
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17978.40434575081
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 17998.2672123909
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 183
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 18018.663904428482
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 18039.599367380142
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 18060.84676861763
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 184
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 18083.905220985413
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 18104.246084690094
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 185
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 18123.435897111893
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 18143.67333173752
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 18164.051960229874
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 186
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END with cost 0.28
current time: 18185.432646036148
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  move} ; C ; END
current time: 18206.437424898148
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  move} ; C ; END
current time: 18226.59162926674
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 187
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 18247.08888745308
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  move} ; C ; END
current time: 18267.561006069183
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 188
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 18288.56473183632
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  move} ; C ; END
current time: 18309.335243701935
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 189
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END with cost 0.28
current time: 18329.382628679276
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
current time: 18348.945661067963
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18368.884321928024
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18388.83829641342
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18408.668219089508
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18428.4982483387
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18448.69050836563
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 190
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 18469.35760998726
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  move} ; C ; END
current time: 18490.196094989777
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  move} ; C ; END
current time: 18512.606561422348
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 191
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 18534.187980890274
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18554.402688264847
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 192
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 18574.93533349037
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18595.477566957474
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18617.029954195023
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 193
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 18637.507049560547
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18657.433777093887
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18678.310898542404
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18699.19259762764
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18719.414371728897
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18739.384811639786
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18759.254356861115
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 194
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 18779.792496681213
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18800.694386720657
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 195
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 18821.283545732498
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18841.93159365654
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 18864.476246118546
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 196
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END with cost 0.28
current time: 18884.668672323227
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
current time: 18904.689552783966
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18924.905642271042
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18944.767869472504
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18964.485070943832
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 18984.213671922684
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19004.109641075134
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 197
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 19024.177955150604
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  move} ; C ; END
current time: 19044.49111843109
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 198
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END with cost 0.28
current time: 19064.760918855667
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  move} ; C ; END
current time: 19085.370568037033
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  move} ; C ; END
current time: 19106.257127285004
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 199
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 19126.70399928093
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  move} ; C ; END
current time: 19148.194785118103
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 200
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 19168.328863859177
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  move} ; C ; END
current time: 19189.09517455101
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  move} ; C ; END
current time: 19210.08349776268
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 201
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 19232.81666493416
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19253.33637189865
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 202
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 19273.25410604477
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19293.080722332
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19313.066245794296
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19332.58373451233
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19352.73824429512
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19372.962099552155
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19393.016018629074
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 203
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 19413.123937129974
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19435.470237970352
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19456.46394085884
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 204
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 19477.503976106644
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19497.832832098007
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 205
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 19517.89545226097
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19538.939292430878
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 19559.22607111931
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 206
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 19580.47837471962
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { move} ; C }  move} ; C }  move} ; C ; END
current time: 19602.951981306076
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_left C } ; C }  move} ; C }  move} ; C ; END
current time: 19624.283062696457
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { WHILE(not (front_is_clear)) { turn_right C } ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 207
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  move} ; C ; END with cost 0.28
current time: 19645.117839813232
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  move} ; C ; END
current time: 19664.81408238411
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  move} ; C ; END
current time: 19686.173065423965
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 208
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  move} ; C ; END with cost 0.28
current time: 19706.79555797577
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  move} ; C ; END
current time: 19727.166722536087
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 209
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END with cost 0.28
current time: 19747.451625823975
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  move} ; C ; END
current time: 19767.522678613663
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_right WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19787.592221021652
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { turn_left WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19807.332228660583
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19827.469144821167
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_right WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19847.839846134186
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { turn_left WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
current time: 19867.661768198013
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right IF(not (right_is_clear)) { WHILE(not (right_is_clear)) { C } ; C }  move} ; C }  move} ; C ; END
[ITER] 210
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  move} ; C ; END with cost 0.28
current time: 19887.868344783783
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  move} ; C ; END
current time: 19908.274601459503
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  move} ; C ; END
------ end current search -------

[ITER] 211
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 19928.70169377327
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 19949.74064898491
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 19970.806244134903
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 19991.878930091858
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20013.11458182335
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 212
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20038.03886771202
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20060.27464056015
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 213
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20081.84112763405
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20104.839470863342
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20127.809638738632
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 214
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20150.64731836319
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20194.444124937057
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20215.66069340706
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 215
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20236.936982393265
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20258.52551484108
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 216
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20279.263293027878
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20300.237941503525
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20320.747636318207
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20341.31122636795
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20362.78490805626
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
[ITER] 217
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20386.697059869766
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20408.127776384354
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 218
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20428.21451497078
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20451.189217567444
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20474.415142297745
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 219
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20497.631587266922
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20541.648809671402
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20563.104948997498
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 220
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 20584.912353038788
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 20607.566887378693
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 221
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.28
current time: 20628.976323366165
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 20671.672911643982
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 20710.505130290985
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_left WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 20747.92928123474
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
[ITER] 222
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.28
current time: 20786.52112555504
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 20824.397195339203
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 223
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END with cost 0.28
current time: 20862.590302705765
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
current time: 20901.172294855118
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 224
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 20939.185215950012
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 20962.6321516037
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 20985.0469288826
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 225
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21007.822241306305
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21029.482211351395
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 226
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21051.17211818695
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21072.37865281105
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21093.623024225235
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21114.968252182007
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21136.409168958664
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 227
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21160.96802997589
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21181.801560401917
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 228
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21203.079748392105
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21223.197328567505
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21267.80378627777
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 229
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21289.820399045944
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21310.98008084297
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 230
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21331.99441719055
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21352.907756567
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21396.496156454086
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 231
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21418.00690793991
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21439.129893779755
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21460.44116139412
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21481.137694120407
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21502.471052646637
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
[ITER] 232
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21524.974031448364
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21546.257900714874
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 233
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 21567.95528459549
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21591.215941905975
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 21614.038683652878
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 234
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.28
current time: 21637.180992364883
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 21675.076781988144
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 235
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.28
current time: 21713.47202849388
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 21751.54581093788
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 236
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END with cost 0.28
current time: 21790.194689035416
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 21833.413278579712
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 21871.36913919449
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_left WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
current time: 21908.832546949387
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;} }  move} ; C ; END
[ITER] 237
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 21947.261770248413
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 21969.05205798149
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 21989.9401409626
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22011.06050825119
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22032.37569642067
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 238
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22056.822190999985
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22078.2605240345
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 239
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22099.271684408188
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22120.806996822357
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22164.385415315628
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 240
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22185.352801799774
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22206.728162050247
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 241
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22228.30446958542
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22251.721605300903
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22275.272521972656
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 242
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22298.08016896248
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22319.11274790764
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 243
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22340.393763065338
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_right} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22385.410875558853
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22406.103628635406
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 244
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22427.22204041481
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22449.809989452362
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22472.25605082512
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 245
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22495.68183016777
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { move WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22516.573867559433
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22537.828201532364
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { turn_left WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22558.958401203156
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(front_is_clear) { WHILE(front_is_clear) { C } ; C }  turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22581.79088997841
all rewards: [0, 0, 0, 0, 0]
case 3: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (front_is_clear)) { turn_right}  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
[ITER] 246
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 22605.655534267426
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 22627.143547058105
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 247
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.28
current time: 22648.670118808746
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left move C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 22686.924080848694
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_left turn_left C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 248
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.28
current time: 22725.30737876892
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right turn_right C } ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 22761.902410268784
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { turn_right move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
------ end current search -------

[ITER] 249
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END with cost 0.28
current time: 22800.305287361145
all rewards: [0, 0, 0, 0, 0]
case one applied: put into queue and get cost 7 reward 0.0 for seed 0 for 
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 22843.82600569725
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_right WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 22882.31241774559
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 9 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { turn_left WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
current time: 22920.35344338417
all rewards: [0, 0, 0, 0, 0]
add if: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(left_is_clear) { IF(right_is_clear) { WHILE(right_is_clear) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;} }  move} ; C ; END
[ITER] 250
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 22957.846212863922
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 22996.491817474365
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 23034.446393489838
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 251
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 23071.88598227501
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  move} ; C ; END
current time: 23109.43999195099
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  move} ; C ; END
current time: 23146.8402428627
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 252
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 23184.365998506546
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 23221.450397491455
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 23259.036895036697
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 253
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 23297.1502161026
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  move} ; C ; END
current time: 23335.572731256485
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  move} ; C ; END
current time: 23372.437705039978
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 254
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 23410.370206594467
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 23448.89055967331
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 23486.388648033142
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 255
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  move} ; C ; END with cost 0.28
current time: 23525.19099855423
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  move} ; C ; END
current time: 23561.39004278183
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  move} ; C ; END
current time: 23598.70176744461
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  move} ; C ; END
------ end current search -------

[ITER] 256
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 23636.165912389755
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right} ELSE { turn_left WHILE(not (front_is_clear)) { move C } ;}}  move} ; C ; END
current time: 23699.94677567482
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right turn_right C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 257
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 23738.70048069954
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 23776.930543661118
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 23814.994134426117
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
------ end current search -------

[ITER] 258
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 23853.583863019943
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right} ELSE { turn_left WHILE(not (front_is_clear)) { turn_left C } ;}}  move} ; C ; END
current time: 23916.7264213562
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 259
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END with cost 0.28
current time: 23954.85395359993
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 23993.546684980392
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
current time: 24031.923309087753
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_left C } ;}  move} ; C ; END
------ end current search -------

[ITER] 260
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 24070.091679096222
all rewards: [0, 0, 0, 0, 0]
case 2: put into queue and get cost 7 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right} ELSE { turn_left WHILE(not (front_is_clear)) { turn_right C } ;}}  move} ; C ; END
current time: 24133.804781913757
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(not (left_is_clear)) { WHILE(not (left_is_clear)) { move} ; turn_right turn_right C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 261
searching base on  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END with cost 0.28
current time: 24171.90564751625
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { move C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 24210.000133514404
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_left C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
current time: 24247.70037126541
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(not (front_is_clear)) { IF(right_is_clear) { WHILE(right_is_clear) { IF(left_is_clear) { WHILE(left_is_clear) { turn_right C } ; C }  move} ; C }  turn_left WHILE(not (front_is_clear)) { turn_right C } ;}  move} ; C ; END
------ end current search -------

[ITER] 262
searching base on  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END with cost 0.28
current time: 24285.724686145782
all rewards: [0, 0, 0, 0, 0]
expand: put into queue and get cost 8 reward 0.0 for seed 0 for
  WHILE(not (present_goal)) { IF(right_is_clear) { WHILE(right_is_clear) { move} ; turn_right turn_right C }  IF(not (front_is_clear)) { turn_left WHILE(not (front_is_clear)) { move C } ;}  move} ; C ; END
current time: 24334.47765159607

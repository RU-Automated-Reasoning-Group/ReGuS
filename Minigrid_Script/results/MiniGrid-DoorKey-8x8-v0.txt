 WHILE(front_is_clear) { IF(front_is_closed_door) { turn_left}  IF(front_is_key) { turn_left}  move} ; turn_right WHILE(not (goal_on_right)) { IF(left_is_clear) { turn_left}  IF(front_is_key) { pickup}  IF(goal_present) { return}  IF(front_is_closed_door) { IF(not (front_is_clear)) { IF(not (has_key)) { IF(not (right_is_clear)) { turn_right}  IF(not (front_is_key)) { get_key}  pickup}  get_locked_door}  toggle}  IF(not (front_is_clear)) { turn_right}  move} ; turn_right WHILE(not (goal_present)) { move} ;; END (False)